# Copy to `.env` in this folder if you run Vite without the dev proxy
# (e.g. `npm run dev:vite-only` while FastAPI runs on port 8000).
# When unset, the app uses same-origin `/api/...` (Vite proxies to FastAPI in `npm run dev`).
# VITE_API_BASE_URL=http://127.0.0.1:8000
